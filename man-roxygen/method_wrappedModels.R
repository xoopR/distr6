#' @param model `(character(1))` \cr
#' id of wrapped [Distribution](s) to return. If `NULL` (default), a list of all wrapped [Distribution]s
#' is returned; if only one [Distribution] is matched then this is returned, otherwise a list of
#' [Distribution]s.
